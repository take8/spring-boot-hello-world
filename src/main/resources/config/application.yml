spring:
  web:
    resources:
      cache:
        period: 86400  # 24時間
  messages:
    basename: "i18n/messages"
    encoding: "UTF-8"
  datasource:
    username: spring
    password: spring
    url: "jdbc:postgresql://localhost:5432/spring"
